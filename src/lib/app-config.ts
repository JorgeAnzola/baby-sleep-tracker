// This file imports the version from package.json
// Note: In Next.js, we can't directly import package.json in client components
// So we export the version as a constant that can be updated during build

export const APP_VERSION = '0.1.0';
export const APP_NAME = 'Baby Sleep Tracker';
export const DEVELOPER_NAME = 'Sleep Tracker';
